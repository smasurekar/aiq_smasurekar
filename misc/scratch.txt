uv run --project scripts/eval python eval_common/run_aiq_eval.py   \
--dataset-path /localhome/local-smasurekar/smasurekar/blueprint-pipeline/evaluation/rag-eval/scripts/datasets/deepsearchqa  \
--server-url http://localhost:8100 \
--agent adaptive_researcher \
--collection deepsearchqa  \
--skip-ingestion \
--max-workers 8 \
--judge-model nvidia/meta/llama-3.3-70b-instruct \
--judge-endpoint https://inference-api.nvidia.com/v1/chat/completions \
--judge-max-workers 2 \
--limit 100 \
--output-dir results/aiq/deepsearchqa-adaptive-all-post-token-optim-100


python3 aiq_inference.py --server-url http://localhost:8100 "What was Apple sale number for 2021"


python3 aiq_inference.py --server-url http://localhost:8100 "Consider the OECD countries whose total population was composed of at least 20% of foreign-born populations as of 2023 (according to the Observatory of Migration at the university of Oxford). Amongst them, which country saw their overall criminality score increase by at least +0.2 point between 2021 and 2023 and their resilience score decrease by more than 0.3 between these same dates (according to the Organised Crime Index)?"